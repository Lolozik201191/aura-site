# ЭТАП 4.3 — Визуальный аудит текста и вёрстки

## Метод и оговорка
Проведён по-кадровый vision-скан всех 18 страниц (60 кадров, модель `deepseek-v4-flash-vision-exp` через API).
Результат: при массовых запросах модель часто отвечала неструктурно или сбоила (структурно подтвердилось лишь ~10%
ответов). Все замечания **перепроверены по исходному коду (HTML/CSS) и скриншотам**; неподтверждённые отброшены.

## Проверенные замечания vision → статус
1. **about.html** — «заголовок сливается со светлым фоном» — ❌ **не подтверждено**: блок `.legend` тёмный (`#2A2622→#40392F`, белый текст), kicker золотой — контраст высокий.
2. **delivery.html** — «опечатка „есть салонов“» — ❌ **не подтверждено**: во всех футерах корректно «…сеть салонов…».
3. **delivery.html / faq.html** — «заголовки/текст обрезаны» — ❌ **артефакт съёмки**: текст в разметке полный («Условия — в разделе „Акции“» и т.д.); обрезка возникала на границах кадров.
4. Прочие ~50 ответов — неструктурные/сбойные, замечания не подтверждены.

## Подтверждённые и уже исправленные проблемы читаемости (по всем вкладкам)
Эти пункты валидированы кодом и CSS-расчётами ранее:
- ✅ плейсхолдеры поиска/промокода — затемнены (`#6E675C`);
- ✅ нижняя строка футера — контраст повышен (`#C6BFB1`);
- ✅ приглушённые тексты/старые цены — токен `--ink-3` затемнён;
- ✅ золотой текст на светлом (`--gold-d`) затемнён;
- ✅ белый текст на фото: тёмные pill-чипы, scrim сверху/снизу, тёмная зона под текстом в карточках акций, подложки в «AURA в жизни», бейдж свадьбы;
- ✅ мелкие серые метки в карточках — выровнены через токены.

## Что рекомендую дальше
- Прогнать контрольный проход **малыми партиями** (по 2–3 страницы за раз, свежая сессия/сниженные лимиты) — это
  стабильнее, чем 60 кадров подряд;
- Либо вы проходите по серверу и называете конкретные «тонущие» надписи — правлю точечно.
